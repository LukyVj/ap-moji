<% if (canvasSize != false) { %>
<canvas id="c1" width="<%- canvasSize -%>" height="<%- canvasSize -%>" style="display:none">
<% } else { %>
<canvas id="c1" width="400" height="200" style="display:none">
<% } %>
  No support
</canvas>
<span id="mjid" style="display:none;"><%- id -%></span>
<% if (size) { %>
<span id="mjiSize" style="display:none;"><%- size -%></span>
<% } %>
<% if (canvasSize) { %>
<span id="canvasSize" style="display:none;"><%- canvasSize -%></span>
<% } else { %>
<% } %>

<script>
  var getJSON = function(url) {
  return new Promise(function(resolve, reject) {
    var xhr = new XMLHttpRequest();
    xhr.open('get', url, true);
    xhr.responseType = 'json';
    xhr.onload = function() {
      var status = xhr.status;
      if (status == 200) {
        resolve(xhr.response);
      } else {
        reject(status);
      }
    };
    xhr.send();
  });
};
var jsonPath = 'http://'+window.location.hostname+':3000/data/api.json';
</script>